# GGG (Go Git Get)

## Description

Often times, I find myself needing to clone a git repository and then cd into it. This is a simple script that does that for me.

It searches for repositories configuration in the `$HOME/.ggg` file.

## Configuration

The configuration file is.... WiP